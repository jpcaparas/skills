# implicit-token-savings

Thin agent-facing wrapper for the implicit-token-savings skill.

Use this skill when the task benefits from terse repo exploration, compact git inspection, narrow test commands, or low-noise container checks. The canonical instructions live in `SKILL.md`.
