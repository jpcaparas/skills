# Auth Reference Stub

This placeholder exists so the anatomy and patterns references can point to a concrete `references/auth.md` example.

Use it as a minimal example when demonstrating a hub-and-spoke skill layout:

- describe the authentication modes the wrapped system supports
- note the required environment variables or files
- show one tested, low-freedom authentication example
- list the most common auth failures and their fixes
