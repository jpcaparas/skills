# Streaming Reference Stub

This placeholder exists so the anatomy and patterns references can point to a concrete `references/streaming.md` example.

Use it as a minimal example when documenting streaming behaviour:

- explain when streaming is the default versus optional
- show the exact flags or API fields required to enable streaming
- document buffering, chunk ordering, and termination conditions
- capture the common failure modes and verification checks
