# Global Placement Snapshot

Current directory: `/tmp/notes` (not a repository)

Global libraries visible to the inspector:

- `~/.copilot/skills/` contains 4 skills and is the current install family for the running skill.
- `~/.codex/skills/` contains 25 skills but belongs to a different target harness.
- `~/.agents/skills/` is empty.

The requested skill targets the current harness. No project-local destination exists.
