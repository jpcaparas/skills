# Library Policy

Retired skills live under `skills/retired/` for one release and remain absent from active publication surfaces. They may then be deleted when the dependency scan is empty. Historical release notes may retain plain-text mentions, but no active route or install record may resolve the retired name.
