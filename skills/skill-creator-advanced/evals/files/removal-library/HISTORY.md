# Library History

`legacy-notifier` was retired after `notification-auditor` replaced its active notification-audit workflow. This historical sentence is not a catalog, router, dependency, or install route and remains after package removal.
