# Library Policy

Drafts live under `skills/lab/`; published skills live under `skills/stable/`. Promotion requires release validation, passing behavioral evals, no placeholders, and atomic inclusion in `CATALOG.md`, `registry.json`, and the `skill-map` router. Finish with `tool skills list` from a clean checkout.
