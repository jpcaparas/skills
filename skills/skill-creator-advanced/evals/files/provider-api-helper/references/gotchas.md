# API Example Rules

Every HTTP example that calls the provider API must include `Authorization: OrbitToken $ORBIT_TOKEN`. Keep this provider-specific credential name and scheme in this skill rather than general creator guidance.
