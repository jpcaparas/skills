# Library Policy

Published skills live under `skills/stable/`. A deprecated skill moves to `skills/retired/` for one release, is removed from active catalogs, registries, routers, and discovery, and names its replacement. Every dependent must migrate before the move. After one release and a zero-reference scan, the retired package may be removed completely.
