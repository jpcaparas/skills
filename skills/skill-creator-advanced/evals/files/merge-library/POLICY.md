# Library Policy

Published skills live in `skills/stable/` and must be listed in `CATALOG.md`, `registry.json`, and `skills/stable/skill-map/SKILL.md`. A merge must preserve unique behavior, migrate dependents and evals, remove superseded active routes, and end with `tool skills list` plus a stale-name search.
