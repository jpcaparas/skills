{
  "skill_name": "maintainable-tests",
  "created_by": "skill-creator-advanced",
  "evals": [
    {
      "id": 1,
      "name": "account-withdrawal-behavior-tests",
      "prompt": "Add tests for Account::withdraw in tests/AccountTest.php. It should allow withdrawing the available balance, reject one dollar beyond the balance, and keep the original balance when the withdrawal is rejected. Use the existing Pest style.",
      "expected_output": "Writes readable Pest tests with behavior-shaped names, concrete Money values, visible Arrange / Act / Assert shape, and specific assertions for result and balance state.",
      "assertions": [
        {
          "text": "Names tests after withdrawal behavior and outcome",
          "type": "functional"
        },
        {
          "text": "Uses concrete boundary values for exact balance and beyond balance",
          "type": "functional"
        },
        {
          "text": "Asserts both rejection reason and unchanged balance for the failure case",
          "type": "functional"
        }
      ],
      "tags": ["smoke", "behavior", "php", "pest"]
    },
    {
      "id": 2,
      "name": "legacy-rounding-characterization",
      "prompt": "Before refactoring LegacyInvoiceCalculator, add a characterization test for the current pre-2025 tax rounding behavior. Nobody is sure if it is right, but old invoices must not change while we refactor.",
      "expected_output": "Writes the requested characterization test, labels uncertainty, explains why behavior is pinned, and avoids presenting it as a permanent domain rule. Consults legacy guidance only if the preservation decision remains unclear.",
      "assertions": [
        {
          "text": "Consults references/legacy-and-characterization.md only when preservation or uncertainty needs further guidance; does not load unrelated references",
          "type": "disclosure"
        },
        {
          "text": "Includes rationale for preserving current legacy behavior during refactor",
          "type": "functional"
        },
        {
          "text": "Does not treat unknown legacy behavior as a timeless product rule",
          "type": "negative"
        }
      ],
      "tags": ["edge", "legacy", "characterization", "disclosure"]
    },
    {
      "id": 3,
      "name": "hard-to-test-payment-service",
      "prompt": "I need tests for PaymentService. It constructs StripeClient internally, reads Date.now(), and sends receipts in the same method that calculates invoice totals. Please just mock whatever is needed.",
      "expected_output": "Inspects existing safe seams for gateway construction, time, and receipts. Uses framework overrides or scoped, restored doubles where sufficient; otherwise proposes the smallest proportionate production refactor and respects authorization. Tests prove totals and important effects without real charges or email, reusing adequate adapter coverage.",
      "assertions": [
        {
          "text": "Consults references/doubles-and-boundaries.md only for unresolved seam or test-double choices, not as a required prelude to every test",
          "type": "disclosure"
        },
        {
          "text": "Uses an existing safe seam or proposes a necessary scoped refactor, rather than mandating production decomposition before any test",
          "type": "functional"
        },
        {
          "text": "Controls gateway, clock, and receipt effects without real external calls, leaked globals, or incidental call-order coupling",
          "type": "functional"
        },
        {
          "text": "Does not expose private internals only for tests",
          "type": "negative"
        }
      ],
      "tags": ["edge", "boundary", "testability", "mocking"]
    },
    {
      "id": 4,
      "name": "review-vague-mock-heavy-tests",
      "prompt": "Review this diff: tests are named 'works' and 'handles error', most assertions are assertTrue/assertNotNull, and the order repository mock expects five internal calls. Is that okay?",
      "expected_output": "Uses severity-first review, calls out misleading tests, weak assertions, and brittle implementation coupling, then suggests behavior names, observable assertions, and narrower doubles.",
      "assertions": [
        {
          "text": "Consults references/review-rubric.md only for unresolved review judgments; a severity-first review does not require its template",
          "type": "disclosure"
        },
        {
          "text": "Prioritizes behavior gaps and brittleness before style nits",
          "type": "functional"
        },
        {
          "text": "Identifies vague names and weak assertions as maintainability problems",
          "type": "functional"
        }
      ],
      "tags": ["review", "mocking", "assertions", "disclosure"]
    },
    {
      "id": 5,
      "name": "named-parameterized-edge-cases",
      "prompt": "Convert these three copied validation tests into a parameterized test: empty email, malformed email, and email longer than 255 characters. Keep it readable for onboarding.",
      "expected_output": "Creates a parameterized test with named cases that explain each boundary, keeps expected outcomes visible, and avoids an opaque anonymous data table.",
      "assertions": [
        {
          "text": "Uses named dataset or case labels for each email boundary",
          "type": "functional"
        },
        {
          "text": "Keeps the expected validation result visible",
          "type": "functional"
        },
        {
          "text": "Does not hide all behavior in a generic helper",
          "type": "negative"
        }
      ],
      "tags": ["edge", "datasets", "fixtures"]
    },
    {
      "id": 6,
      "name": "review-false-positive-artifact-assertions",
      "prompt": "Review evals/files/publish-config.spec.ts against this contract: production writes formatter.ts with `export default { mode: \"strict\" };`, writes formatter.ts.backup only when backup is true, and the backup must equal the old bytes. Identify tests that can pass while the contract is broken and show the smallest repair.",
      "expected_output": "The agent catches the boolean-versus-content assertion and formatter.json.backup path drift, recommends reading exact destination and backup bytes, and preserves valid backup-content coverage. Additional reference reads depend on an unresolved design question.",
      "files": ["evals/files/publish-config.spec.ts"],
      "assertions": [
        {
          "text": "Consults references/side-effects-and-compatibility.md only if an isolation or assertion decision needs it; does not require extra reads for defects already clear from the supplied contract",
          "type": "disclosure"
        },
        {
          "text": "Identifies that existsSync returns a boolean and therefore cannot prove the previous file contents were replaced",
          "type": "functional"
        },
        {
          "text": "Identifies formatter.json.backup as a different artifact from the production formatter.ts.backup path",
          "type": "functional"
        },
        {
          "text": "Repairs the tests by asserting exact destination and backup contents, not only exit status or existence",
          "type": "functional"
        },
        {
          "text": "Repairs the no-backup case by checking the actual formatter.ts.backup path is absent when backup is false",
          "type": "functional"
        },
        {
          "text": "Does not discard the already exact backup-content assertion",
          "type": "negative"
        }
      ],
      "tags": ["review", "edge", "disclosure", "effects", "assertions"]
    },
    {
      "id": 7,
      "name": "isolate-effects-across-supported-versions",
      "prompt": "Our retry adapter supports SDK 2 through 4. SDK 4 adds an optional scheduleWithoutBlocking method. The current tests make a real HTTP call, call sleep(2), mutate a global SDK singleton, and run only on SDK 4. Redesign the focused tests and bounded CI coverage without hiding required failures behind broad skips.",
      "expected_output": "The agent routes to side-effect and compatibility guidance, denies unstubbed requests, fakes waiting while asserting meaningful backoff, restores global state, tests capability presence and absence, and includes the lowest supported plus current SDK combinations.",
      "assertions": [
        {
          "text": "Consults references/side-effects-and-compatibility.md when non-obvious isolation or version-coverage decisions remain unresolved, without unrelated reference reads",
          "type": "disclosure"
        },
        {
          "text": "Makes unstubbed network access fail and replaces real sleep with a controllable scheduler or clock",
          "type": "functional"
        },
        {
          "text": "Restores the global SDK state so test order cannot affect the result",
          "type": "functional"
        },
        {
          "text": "Tests capability-present and capability-absent paths plus the lowest and current supported SDK combinations",
          "type": "functional"
        },
        {
          "text": "Uses a narrow capability reason for optional skips and fails with context when the requested behavior is required",
          "type": "functional"
        }
      ],
      "tags": ["smoke", "edge", "disclosure", "isolation", "compatibility", "side-effects"]
    },
    {
      "id": 8,
      "name": "negative-disposable-port-check",
      "prompt": "Give me a one-off shell command to test whether localhost port 8080 is open. I am not changing product code, do not want an automated test, and will discard the command immediately.",
      "expected_output": "The skill should not trigger because the word test describes a disposable manual probe, not automated tests or durable test code.",
      "assertions": [
        {
          "text": "Does not load maintainable-tests or produce fixture, mocking, coverage, isolation, or compatibility guidance",
          "type": "negative"
        }
      ],
      "tags": ["negative", "near-miss"]
    },
    {
      "id": 9,
      "name": "tests-only-receipt-replay-regression",
      "prompt": "Add only a pytest regression in tests/test_billing_service.py; production files are out of scope. billing/service.py imports Gateway from payments_sdk, time from time, and send_receipt from mailer. Its fixed capture(invoice) is: `if invoice.paid: return invoice.charge_id`; otherwise it calls `Gateway().charge(invoice.total_cents, time())`, assigns the returned ID to invoice.charge_id, sets invoice.paid = True, calls `send_receipt(invoice.email, invoice.charge_id)`, and returns the ID. The bug was sending a second receipt on a replay. Existing tests cover the first successful capture and SDK adapter contract. The suite uses pytest monkeypatch with automatic teardown and denies unstubbed network. Use an invoice for billing@example.test, 1250 cents, initially unpaid with no charge ID; invoke capture twice. Do not redesign PaymentService or add constructor injection.",
      "expected_output": "Adds the replay regression using scoped monkeypatches on billing.service.Gateway, billing.service.time, and billing.service.send_receipt, with restored state and no real payment or email. Proves identical returned charge IDs, paid state, one exact charge and one receipt. Leaves production and adequate first-capture/adapter coverage unchanged.",
      "assertions": [
        {
          "text": "Changes only the requested test surface and does not require or implement a production refactor",
          "type": "negative"
        },
        {
          "text": "Uses module lookup seams with reliable pytest cleanup, deterministic time, and denied unstubbed network rather than real gateway or mail effects",
          "type": "functional"
        },
        {
          "text": "Asserts identical capture results, paid state and stored charge ID, exactly one charge with 1250 cents and controlled time, and exactly one receipt to billing@example.test for that charge ID",
          "type": "functional"
        },
        {
          "text": "Shows why the regression would fail if the replay resent a receipt, runs or identifies the focused pytest check, and does not duplicate adequate existing success or adapter tests",
          "type": "verification"
        }
      ],
      "tags": ["edge", "regression", "tests-only", "boundary", "isolation", "scope"]
    },
    {
      "id": 10,
      "name": "small-test-edit-no-extra-ceremony",
      "prompt": "Rename only the vague test title in tests/invitations.spec.ts: `it('handles error', () => { expect(validateInvitation({ expiresAt: 100 }, 101)).toEqual({ ok: false, reason: 'expired' }); });`. Existing tests already cover the valid and exact-deadline cases. The test body is correct. Keep this a small edit and summarize briefly.",
      "expected_output": "Renames the test to describe rejection after expiry, preserves the exact assertion and body, uses focused existing verification, and gives a brief outcome without extra reference reading or ceremony.",
      "assertions": [
        {
          "text": "Changes only the title to describe the post-deadline rejection and preserves the exact observable assertion",
          "type": "functional"
        },
        {
          "text": "Does not add tests, fixtures, production refactors, a written behavior story, or a multi-section review report",
          "type": "negative"
        },
        {
          "text": "Does not read reference stacks when the supplied test and contract already resolve the naming decision",
          "type": "disclosure"
        },
        {
          "text": "Runs relevant existing checks or reports a concrete verification limitation without claiming unrun tests passed",
          "type": "verification"
        }
      ],
      "tags": ["smoke", "small-edit", "disclosure", "scope"]
    }
  ]
}
