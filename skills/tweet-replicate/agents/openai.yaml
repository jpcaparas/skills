interface:
  display_name: "Tweet Replicate"
  short_description: "Deterministic MP4 plus capped GIF replicas for public X/Twitter posts"
  default_prompt: "Replicate this public tweet into an MP4 and a GIF under 24 MB: "

policy:
  allow_implicit_invocation: false
