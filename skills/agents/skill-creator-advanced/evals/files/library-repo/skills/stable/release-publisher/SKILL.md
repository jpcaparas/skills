---
name: release-publisher
description: "Validate and publish an authorized release after release notes are approved."
---

# Release Publisher

1. Invoke `release-notes` to prepare the release copy.
2. Run the repository dry run.
3. Publish only when the user has authorized the external effect.
