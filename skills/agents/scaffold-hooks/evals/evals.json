{
  "evals": [
    {
      "id": 1,
      "name": "universal_four_harness_scaffold",
      "prompt": "Run /scaffold-hooks in this repo. Scaffold Claude Code, Codex, Devin CLI, and OpenCode hook configuration into one shared hooks/ directory without duplicating event behavior.",
      "expected_output": "The response audits existing hook configs, uses the universal scaffold-hooks workflow, creates shared hooks/<event>/script.sh behavior with claude/codex/devin adapters, enables opencode-froggy in opencode.json with hooks in .opencode/hook/hooks.md, preserves unrelated custom hooks, and verifies no selected shell harness config points at legacy generated roots.",
      "files": [
        "SKILL.md",
        "scripts/scaffold_all_hooks.sh",
        "templates/hook-plan.example.json",
        "references/collision-policy.md"
      ],
      "assertions": [
        {
          "text": "Uses one shared hooks/ tree for shell harness event behavior"
        },
        {
          "text": "Strips .claude/.codex/.devin generated-root command entries before appending new adapters"
        },
        {
          "text": "Preserves non-managed custom hook entries"
        },
        {
          "text": "Uses opencode-froggy for OpenCode hook scaffolding"
        }
      ]
    },
    {
      "id": 2,
      "name": "legacy_migration_no_collision",
      "prompt": "This repo already has .claude/hooks/generated and .codex/hooks/generated entries. Run /scaffold-hooks and make the configs follow the shared hooks/ pattern without collisions.",
      "expected_output": "The response treats the work as a migration, removes only managed legacy generated-root command entries, scaffolds replacement adapters under hooks/, leaves custom hooks alone, and reports verification that old generated-root commands no longer remain.",
      "files": [
        "scripts/scaffold_all_hooks.sh",
        "references/migration.md"
      ],
      "assertions": [
        {
          "text": "Legacy generated folders are deleted only when they contain a managed manifest"
        },
        {
          "text": "Final configs are scanned for legacy generated-root references"
        },
        {
          "text": "Shared scripts are not overwritten by later harness scaffolders"
        }
      ]
    },
    {
      "id": 3,
      "name": "single_harness_subset",
      "prompt": "Only scaffold Codex hooks for this repo. Do not touch Claude, Devin, or OpenCode.",
      "expected_output": "The response uses this skill with --harnesses codex so only the Codex adapter and config are written, leaving Claude, Devin, and OpenCode files untouched.",
      "files": [
        "SKILL.md"
      ],
      "assertions": [
        {
          "text": "Only the requested harness is scaffolded via --harnesses"
        },
        {
          "text": "Other harness config files are not created or modified"
        }
      ]
    },
    {
      "id": 4,
      "name": "project_specific_stop_check",
      "prompt": "Use /scaffold-hooks and make every harness call scripts/agent-stop-checks.sh at Stop without copying that validation logic into four hook files.",
      "expected_output": "The response writes a universal plan with Stop scripts entries for Claude, Codex, and Devin, renders OpenCode Froggy bash actions that call a repo-owned validation script, and leaves validation policy in scripts/agent-stop-checks.sh.",
      "files": [
        "templates/hook-plan.example.json",
        "references/plan-format.md",
        "references/harness-composition.md"
      ],
      "assertions": [
        {
          "text": "Project validation belongs in repo-owned scripts, not duplicated adapter bodies"
        },
        {
          "text": "The plan can pass harness arguments to the same shared script"
        },
        {
          "text": "Generated Stop adapters can skip expensive checks when no configured source extensions changed"
        }
      ]
    }
  ]
}
