# One-Shot Prompt

**Theme**: {{TYPE_LABEL}}
**Route**: {{ROUTE_PATH}}
**Generated**: {{DATE}}
**Model**: {{MODEL}}
**Harness**: {{HARNESS}}
**Mode**: {{MODE}}

## Prompt

Write complete HTML, CSS, and JavaScript in a single self-contained `index.html` file for an immersive one-shot website about:

{{STYLE_BRIEF}}

Requirements:

- Produce one static HTML file only.
- Use no external images and no framework runtime.
- Use CSS, inline SVG, canvas, procedural geometry, and vanilla JavaScript for all visuals.
- Include at least five distinct sections.
- Include scroll-triggered reveals, depth or parallax, one thematic motion system, and a surprising but relevant section.
- Include responsive mobile and desktop layouts.
- Include `prefers-reduced-motion` handling.
- Use a restrained fictional brand name, not a pun.
- Make the route feel like a finished brand experience, not a generic landing page.

## Notes

- Type: `{{TYPE}}`
- Catalog path: `{{ROUTE_PATH}}`
- Fairness mode: isolated route context; no retries unless the manifest marks this route curated.
