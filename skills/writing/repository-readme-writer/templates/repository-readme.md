# Project Name

One-sentence description of what the project does and who or what it is for.

## Quickstart

Install dependencies:

```bash
<install-command>
```

Create local configuration if the project requires it:

```bash
<config-command>
```

Run the project:

```bash
<dev-command>
```

Open the local URL or run the verification command shown by the project.

## Project Shape

Describe the main apps, services, packages, or boundaries in stable language. Avoid a directory tour unless the paths are part of the public interface.

## Configuration

List only the configuration needed for local development or deployment. Group settings by purpose instead of dumping every environment variable.

## Local Development

Add the few common workflows that contributors need after quickstart. Keep command lists short.

## Quality

Run the shared project gate:

```bash
<check-command>
```

## Deployment

Summarize the deployment model and required external services when discoverable.

## Troubleshooting

Document common first-run failures only.
