# instagram-replicate

Public installable skill for rebuilding one public Instagram video post or reel into a deterministic local replica with a frozen snapshot, desktop-style HTML/CSS rebuild, MP4 output, and a companion GIF capped under 24 MB.

`SKILL.md` is the canonical instruction file. The surrounding files package the skill for repository discovery and installation.
