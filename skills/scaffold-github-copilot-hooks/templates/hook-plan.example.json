{
  "hooks_target": ".github/hooks/copilot-hooks.json",
  "managed_root": ".github/copilot/hooks/generated",
  "mode": "additive",
  "enabled_events": [
    {
      "name": "preToolUse",
      "matcher": "bash|edit|create",
      "timeoutSec": 15,
      "block_on_failure": true,
      "scripts": [],
      "commands": [],
      "notes": "Use this for hard policy and safety gates. Deny through stdout JSON, not a Devin-style exit-code-2 block."
    },
    {
      "name": "postToolUseFailure",
      "timeoutSec": 30,
      "block_on_failure": false,
      "scripts": [],
      "commands": [],
      "notes": "Use this for recovery guidance after failed tools."
    },
    {
      "name": "agentStop",
      "timeoutSec": 30,
      "block_on_failure": true,
      "scripts": [
        {
          "label": "shared stop checks",
          "path": "scripts/agent-stop-checks.sh",
          "args": ["copilot"],
          "cwd": ".",
          "notes": "Reusable repo-owned script that can also be called by CI or other agent adapters."
        }
      ],
      "commands": [],
      "notes": "Use this to keep the agent from stopping before required repo checks are complete."
    }
  ]
}
