#!/usr/bin/env sh

# Literal search with line numbers.
alias rgl='rg -n -F'

# Recursive filename listing.
alias rgf='rg --files'

# JSON output for automation.
alias rgj='rg --json --color never --no-heading'

