# jev-opportunities

Keep `SKILL.md` authoritative; wrappers only describe packaging.
This package owns explicit Jev opportunity discovery and bounded spikes, not
production integration. The docs scraper uses no credentials. Keep its tests
offline, and do not confuse structural eval validation with model execution.
