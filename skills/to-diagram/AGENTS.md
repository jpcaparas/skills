# to-diagram

The canonical behavior lives in `SKILL.md`.

Produce one evidence-grounded Mermaid diagram and exactly two durable deliverables: matching `.md` and `.png` files. Prefer a readable overview over exhaustive graph density, preserve material branches and uncertainty, and verify the rendered PNG before handoff.

Use `scripts/render_diagram.py` for deterministic Markdown-to-PNG export. Keep data charts, image-only infographics, and surface-specific Mermaid syntax repair outside this skill's general process-modeling ownership.
