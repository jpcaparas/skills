# travel-plan-spreadsheet-generator

`SKILL.md` is authoritative.

Use this skill when the user explicitly wants a travel-planning workbook or spreadsheet output. Build a canonical trip model first, keep clarification to one consolidated batch, and rely on `scripts/build_workbook.py` for deterministic workbook generation.
