#!/usr/bin/env sh

# Match the GitHub Actions entrypoint. A narrower pre-push check can pass while
# repository-wide skill budgets or package validators still fail in CI.
exec "$(dirname -- "$0")/../scripts/run-pnpm.sh" run validate
